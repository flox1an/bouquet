# Set to "1" to skip broadcasting file/audio/video Nostr events
# (kind 1063/31137/34235/34236) to relays during local testing.
# Events are still signed and returned to the UI; only the relay
# broadcast is suppressed. Leave unset for normal operation.
# VITE_DISABLE_EVENT_PUBLISH=1
